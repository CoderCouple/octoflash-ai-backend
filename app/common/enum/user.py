"""Deprecated — user role is now OrgRole (per-org membership), see
`app.common.enum.org.OrgRole`. This module is kept empty as a no-op to avoid
import errors in legacy code paths; remove once nothing references it.
"""
